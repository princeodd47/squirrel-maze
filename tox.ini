# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py36
[testenv]
changedir=test/unit
deps=
    -rrequirements.txt
    -rtest-requirements.txt
commands=
    py36-test: {[pytest]} test
